spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: dummy
            client-secret: dummy
            scope: openid
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/keycloak
        provider:
          keycloak:
            issuer-uri: http://localhost:8180/realms/task-management
app:
  security:
    enabled: false
